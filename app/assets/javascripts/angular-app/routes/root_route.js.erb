app.config(["$routeProvider", function ($routeProvider) {
  return $routeProvider.when("/", {
    templateUrl: "<%= asset_path('angular-app/templates/records/index.html') %>",
    controller: "RecordsIndexController"
  });
}]);